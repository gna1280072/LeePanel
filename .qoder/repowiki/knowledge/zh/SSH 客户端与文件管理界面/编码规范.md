- 使用 `useRef` 存储会话 ID 或配置状态，确保在异步事件监听器（如 `listen`）中能访问到最新的值而不触发重复订阅。
- 通过 `useState` 控制自定义模态框（如确认对话框、提示输入框）的显示与数据，替代浏览器原生的 `confirm` 和 `prompt` 以保持 UI 风格统一。
- 在涉及文件路径操作时，统一处理根路径 `/` 的特殊情况，通过三元表达式动态拼接绝对路径。