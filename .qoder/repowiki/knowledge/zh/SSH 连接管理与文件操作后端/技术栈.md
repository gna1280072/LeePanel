- Tauri 2.11.2 作为桌面应用框架，前端构建钩子绑定 `npm run dev/build`（见 `tauri.conf.json`）。
- SSH/SFTP 基于 `russh 0.45` + `russh-sftp 2`，异步运行时为 `tokio 1`（见 `Cargo.toml`）。
- 配置持久化使用 `serde/serde_json` 序列化到 `dirs::config_dir()` 下的 `ssh-tool/connections.json` 与 `settings.json`。