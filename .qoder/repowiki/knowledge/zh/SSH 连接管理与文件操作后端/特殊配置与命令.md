- 开发启动需先满足前端构建前置命令：`beforeDevCommand: npm run dev`、`beforeBuildCommand: npm run build`（见 `tauri.conf.json`），再执行 `cargo tauri dev` 或 `cargo tauri build`。
- 日志仅在调试模式启用：`cfg!(debug_assertions)` 时加载 `tauri-plugin-log`（见 `lib.rs::setup`）。