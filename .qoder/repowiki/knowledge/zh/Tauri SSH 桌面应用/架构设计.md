- 采用 Tauri 2 架构，通过 `tauri.conf.json` 定义前端构建流程（Vite）与后端 Rust 应用的绑定关系。
- 前端通过 `@tauri-apps/api` 调用后端暴露的 Rust 命令（Commands），实现 SSH 会话管理与文件操作。
- 后端使用 `russh` 库处理底层 SSH/SFTP 协议，并通过 `tokio` 异步运行时支撑高并发连接。
- 安全隔离由 Tauri 的能力系统（Capabilities）管理，限制前端对系统资源的访问权限。